//! Unit tests migrated out of `src/`.
#![cfg(feature = "test-support")]

mod unit {
    pub mod cli;
    pub mod command;
    pub mod config;
    pub mod conversation;
    pub mod migration;
    pub mod paths;
    pub mod prompts;
    pub mod release_notes;
    pub mod support;
    pub mod update;
}
