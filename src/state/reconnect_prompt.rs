//! Reconnect prompt mode state type (new architecture).

/// Reconnect prompt mode - editing a prompt after choosing to reconnect to an existing worktree.
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct ReconnectPromptMode;
