//! Child prompt mode state type (new architecture).

/// Child prompt mode - typing the task/prompt for child agents.
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct ChildPromptMode;
