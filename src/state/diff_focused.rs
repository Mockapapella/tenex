//! Diff focused mode state type (new architecture).

/// Diff focused mode - keystrokes are handled by the interactive diff view.
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct DiffFocusedMode;
