//! Terminal prompt mode state type (new architecture).

/// Terminal prompt mode - typing a startup command for a new terminal.
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct TerminalPromptMode;
